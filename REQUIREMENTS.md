# Requirements — Iron Circle repo

Aaron's requirements for moving the table's campaign material out of Claude Projects + Obsidian and into this repo. Recorded 2026-09-04 after the original list was lost to an app crash; status refreshed 2026-09-04 after the first deploy. This file is the source of truth; if anything in `CLAUDE.md`, `README.md`, `mkdocs.yml`, or the migration script disagrees with it, this wins.

## The five requirements

1. **A repo with a wiki-facing site hostable on GitHub Pages.** Plain Markdown in the repo, built into a website automatically on push.

2. **Public player docs and "hidden" GM docs share the same repo.** Player-facing pages publish to the site. GM-only pages stay in the repo but never appear on the site. (Hidden means unpublished, not encrypted — the repo is public. Acceptable for now; see the GitHub Pro note in README if that ever changes.)

3. **Contributors can see and edit the files as living docs.** Players and co-GMs edit without needing local tooling — GitHub's web editor is enough. No paywall to contribute (the reason Obsidian was abandoned).

4. **Migrate the Obsidian vault**, minus Obsidian-specific files and the Hot Springs Eternal material.
   - Bring: Cadwallon, Fate Foretold, Documentation.
   - Leave: `.obsidian/`, `Projects/` (Hot Springs Eternal), `TTRPG/` (stale duplicate), commercial PDFs.

5. **A good file structure for tracking a TTRPG campaign.** The Cadwallon vault is the reference point for what works (numbered folders, a schema, reference docs), but the structure is open to revision — suggestions welcome.

## Where each requirement is implemented

| Req | Implemented by | Status |
|---|---|---|
| 1 | `mkdocs.yml` (Material theme), `.github/workflows/deploy.yml` | **Live.** Pages source = GitHub Actions; site at https://randolin.github.io/IronCircle/ (first push build failed before Pages was switched to Actions; manual dispatch succeeded 2026-09-04) |
| 2 | `exclude_docs: gm/` in `mkdocs.yml` + `hooks/exposure.py` honoring `exposure: secret` | **Verified live:** `/stillwater-rise/` → 200, `/stillwater-rise/gm/` and `/stillwater-rise/gm/danger-board/` → 404 |
| 3 | **Sveltia CMS at `/admin/`** (`docs/admin/`, config generated by `tools/build-cms-config.py`) — in-browser editor, Save = commit; plus `edit_uri` pencil icon → GitHub web editor as the no-forms fallback | Built; **OAuth relay not yet deployed** (see README “Editor setup”) — `/admin/` won't sign in until `backend.base_url` is set |
| 4 | `tools/migrate-from-obsidian.ps1` | **Done** — commit `2225f99`; `docs/Cadwallon/`, `docs/Fate Foretold/`, `docs/Documentation/` populated |
| 5 | `docs/stillwater-rise/` layout (public `index.md` + `gm/` with hub, campaign, danger-board, decisions, themes, roster by tier, sessions); `.claude/commands/wrap-session.md` runs the end-of-session workflow | Built for Stillwater Rise; Cadwallon keeps its own schema; no cross-campaign structure decided yet |

## Open questions on requirement 5

- Should Cadwallon and Fate Foretold adopt the Stillwater Rise `gm/` split, or keep their `exposure:` frontmatter approach? Both work with the build; the question is which is easier to maintain.
- Is there a shared structure worth imposing across campaigns (e.g. every campaign gets `index.md` public, `gm/index.md` with a CURRENT STATE block, `gm/decisions.md`, `gm/sessions/`)?
- Where do system references live — `docs/Documentation/` as-is, or split per system?

## Next steps, in order

1. **Retrieve the Stillwater Rise campaign doc** (draft-2, *The Rat Line*) from the Aug 21 chat's file card in the Noir Detective Campaign project; paste into `docs/stillwater-rise/gm/campaign.md`; work the reconciliation checklist there. This is the blocker — nothing downstream can be planned without it.
2. **Deploy the editor's OAuth relay** (README → “Editor setup”): GitHub OAuth App → Cloudflare Worker → set `base_url` in `tools/build-cms-config.py`, re-run, commit. Then open `/admin/`, edit one Tier C entry, confirm the commit lands and the frontmatter survives intact.
3. Decide the open questions above.
4. Create player characters.
5. Plan session 1 (`gm/sessions/session-01.md`).
6. Fill in the publisher links in `docs/Documentation/index.md`.
